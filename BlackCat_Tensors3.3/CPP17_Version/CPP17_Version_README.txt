///

This folder stores versions of code that utilizes C++17  (mainly if-constexpr)
CUDA NVCC currently doesn't not support if-constexpr so workarounds are required, once cpp17 is supported, 
these files will replace their respective work-arounds. 

