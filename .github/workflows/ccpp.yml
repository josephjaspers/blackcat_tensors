name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: configure
      run: cd tests
    - name: make
      run: make default
    - name: run_tests
      run: ./blackcat_tests
